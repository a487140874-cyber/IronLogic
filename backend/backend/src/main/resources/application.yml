spring:
  application:
    name: ironlogic-backend
  datasource:
    url: jdbc:postgresql://localhost:5432/ironlogic
    username: ironlogic
    password: ironlogic
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: true
    locations: classpath:db/migration
  jackson:
    time-zone: UTC
    serialization:
      write-dates-as-timestamps: false

server:
  port: 8080

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    banner: false

logging:
  level:
    root: INFO
    com.ironlogic: INFO
